{
  "name": "novacore-os",
  "description": "NovaCore-OS — Repo-Tooling (Testsuite) der Claude-Code-Plugin-Familie. Versionen leben je Plugin in plugins/<name>/.claude-plugin/plugin.json; dieses Manifest trägt bewusst keine.",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test plugins/nc/tests/*.test.mjs"
  },
  "license": "UNLICENSED"
}
