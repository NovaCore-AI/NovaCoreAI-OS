# Journal 2026-08-25

## 00:1x–01:5x — Nachtschicht: Onsite-Delta Phase I komplett → PR #25 (autonomer /goal-Lauf)

- **Weisung (/goal):** Phase I nach Bauplan und Nachtschicht-Ausführungsplan bauen, PR gegen
  `main` öffnen, **nicht** mergen. Vier Regeln R1–R4 aus dem Loop-Vorfall der Vornacht:
  kein Selbst-Loop, kein Kommando-Ersatz, Blockade ist ein Ergebnis, kein Gate deaktivieren.
- **Startlage geprüft:** PR #24 war gemergt, `main` auf **0.12.0** → von `main` abgezweigt.
  Onsite `origin/main` stand unverändert auf **`2530ced`** (Kern 0.26.0) — exakt der
  Planungsanker, kein Zuwachs zu bewerten. Alle Ports live gegen diesen Stand gelesen.
- **Fünf Commits in Zwangsreihenfolge**, jeder einzeln grün verifiziert:
  `e7e5f19` I-A (Fugen A1–A10) · `7a4db3a` I-B (Umzug B1–B6) · `bcb3f6b` I-C (Mechanik C1–C8) ·
  `80d5a95` Nachzug + Kern-Bump 0.13.0 · `8bd68c1` Waypoint-Schnitt.
- **PR #25 offen**, `MERGEABLE`, **alle 10 CI-Checks grün** (Ubuntu + Windows × Node 20/22/24,
  Plugin-Validierung, CodeQL). Suite 312 → **315**, 0 fail. Kein Merge, kein Tag.

### Entscheidungen, die ich als Overseer getroffen habe

- **A8 aufgeteilt.** Die permissiven Testausnahmen (`JOURNAL_RE`, `VORLAGE_RE`) kamen nach I-A
  — sie matchen dort noch nichts und erfüllen I-3 („kein `git mv` vor der zugehörigen
  Test**ausnahme**"). Die strukturfordernden Invarianten mussten mit dem Umzug in **einen**
  Commit, weil sie vorher unmöglich grün sein können und „Suite grün je Commit"
  Abnahmebedingung ist. Der Bauplan liest sich, als gehöre beides nach I-A; das ist in sich
  widersprüchlich.
- **`2026-08-16-nc-development-modernisierung-bauplan.md` NICHT archiviert**, obwohl AP-F1/F2
  in v0.10.0 vollzogen sind: Er ist alleiniger Träger von **sechs offenen
  „Kommende Änderungen"-Merkern**, und `bauplan-archiv/` ist per Norm terminal („keine Quelle
  Richtung Kern"). Archivierung hätte die Merker unerreichbar gemacht. Liegt jetzt in
  `aktive-bauplaene/`; Registerzeile für die saubere Auflösung angelegt.
- **A7 fasst die frozen v0.1.0-Spec nicht an.** Sie trägt eine `Spec-Version`-Fußzeile, aber
  „Historisch bleibt historisch" (AGENTS.md) schlägt die neue Spec-Governance. Die
  Versionszählung habe ich stattdessen in der **lebenden** Spec
  `2026-07-28-multi-plugin-architektur-design.md` entfernt und die Ausnahme im SSOT-Index
  begründet, damit niemand sie als Drift „repariert".
- **T19 nicht erzwungen.** Der Bauplan verlangt, `end-session` gegen den „Router-Kontext-Deckel
  (6.000 Zeichen, D7)" zu prüfen. Der Deckel existiert, misst aber die **Summe der vier
  `wissen-*`-Descriptions**, nicht einen Skill-Rumpf. Ich habe ihn nicht auf eine Datei
  angewandt, für die er nie galt — und stattdessen die reale Größe berichtet
  (`end-session` 19.472, `start` 15.173 Zeichen).

### Zwei Produktfehler geheilt

1. **`end-session` prüfte gegen einen aufgehobenen Entscheid** (Bauplan §1.1): Der ausgelieferte
   Skill verlangte „`git status --short` zeigt keine `.nc/`-Pfade" und schlug eine
   `.gitignore`-Änderung vor — obwohl `242b9e7` `.nc/` bewusst trackt.
2. **Der Session-Start-Hook hätte gelogen.** Er zog „Laufende Vorhaben" aus `grundwissen/`;
   nach dem P-E1-Schnitt liegen dort nur noch Design-Specs. Ohne Nachzug hätte er in **jeder**
   Sitzung abgeschlossene Specs als laufend präsentiert. Selbst gefunden beim C6-Bau, Test
   trägt jetzt die Gegenprobe.

### Delegation (Org-Ruleset)

- **B3 Fundstellen-Sweep** → Opus-Agent mit Plan-Sandwich. Hat **20 Fundstellen bewusst liegen
  gelassen** statt zu raten (sie sprechen über fremde Arbeits-Repos, deren Regel erst I-C
  schuf) — genau richtig. Zwei vorgelegte Grenzfälle entschieden; eine Matrix-Zeile korrigiert,
  die `README.md` als Nachzugsziel führte, obwohl README `vorlagen/` gar nicht nennt.
- **§7 Nachzugs-Bündel** → Sonnet-Agent. Hat zwei Stellen außerhalb seiner Liste angefasst und
  vorgelegt — beide zu Recht (`nc-sync.md` zeigte als **ausgelieferte Payload** noch den alten
  `.nc/erinnerung/`-Baum). **Zwei Live-Ansprüche habe ich beim Review selbst nachgezogen:**
  die WP0-Zeile derselben Payload und den Kommentar in `nc-end-stempel.js` („künftig eine
  Wissensbasis-Kategorie" — seit diesem PR der Ist-Zustand). An der Stempel-Logik nichts
  geändert (Härtungs-Erhalt I-1).

### Verifikation

- **T1–T7 als echte Negativproben** in je einem frischen Klon gefahren (nicht behauptet):
  T2/T3/T5/T6/T7 schlagen rot an, T1/T4 bleiben grün — die Ausnahmen greifen genau dort, wo
  sie sollen. T8–T14, T16–T18 am gebauten Text belegt.
- Das FFG hat während des Laufs **zwanzig** Schreibzugriffe und mehrere Destruktiv-Kommandos
  gegated. Kein einziges Mal deaktiviert (R4). Statt `rm -rf` für die Probe-Klone habe ich auf
  eindeutige Verzeichnisnamen umgestellt — das Gate zu umgehen wäre einfacher gewesen als
  richtig.
- Der Plugin-Grenzen-Test hat **drei** unqualifizierte `knowledge-base/`-Nennungen in
  ausgelieferten Dateien gefangen. Alle drei geheilt, keine einzige durch Entschärfen des
  Tests.

### Offen an den Maintainer (im PR-Body ausformuliert)

1. **`referenz/`-Review-Belege (P-E5):** Je Datei existiert ein dokumentierter Review-Vorgang,
   aber PR #3/#20/#21 stehen alle auf `REVIEW_REQUIRED` mit **null** aufgezeichneten Reviews.
   Gilt ein Agenten-Review als Abnahme?
2. **Jira-Ablageort:** `.nc/` steht wieder in `.gitignore` (A10) — künftige Jira-Artefakte des
   parallelen Vorgangs wären ungetrackt. `git add -f` als Ausweg im Kommentar vermerkt, Strang
   nicht angefasst.
3. **Abnahme-Vorbehalt S1:** Merge = Wortlaut-Abnahme der Kriterienliste v2 und GL1–GL5.
4. **`/nc:end-session` war in dieser Session nicht aufrufbar** — kein `nc:`-Namespace, das
   Plugin wird hier entwickelt, nicht ausgeführt. Nach R2 kein Ersatz-Kommando genommen; die
   Prozedur habe ich von Hand ausgeführt (dieses Journal, Stand, Roll-up, Register, Memory).

## Nachmittag — N5, Phase-J-Bauplan, Struktur-Paritätsaudit (Overseer Fable)

- **PR #26** (`docs/onsite-delta-phase-j`, Basis #25): Mapping-Nachtrag N5 (Onsite 0.27.0,
  Anker `a9927b2`, D31–D37; Opus-Agent, Overseer-reviewt — Korrektur: neun registrierte
  Hooks, nicht elf) + Phase-J-Bauplan (fünf Pakete J-D → J-A ∥ J-B → J-C → J-E, J-E1–J-E10,
  T1–T21). Push mit Maintainer-Freigabe („bring deine Sachen in eine PR").
- **Struktur-Paritätsaudit → PR #27** (`chore/onsite-struktur-paritaet`, gestackt auf #26),
  Maintainer-Auftrag: „ob wir die Pendants zur Onsite-Struktur eingehalten haben … Kern-SSOT
  ist noch nicht richtig aufgebaut". Befunde und Behebung:
  - `CLAUDE.md` war un-getrackt; Claude Code liest laut Doku `CLAUDE.md`, nicht `AGENTS.md`
    → jeder frische Klon ohne Pflicht-Einstieg. Getrackte `CLAUDE.md` mit `@AGENTS.md`-Import,
    `.gitignore` → `CLAUDE.local.md`, Test-Wächter.
  - Team-Sync-Payload `nc-sync.md` → `doks/nc-teamsync.md` (D13-Parität; Sonnet-Sweep, ~30
    lebende Fundstellen, Hook, Tests, Pfad-Index, `hooks.json`-description).
  - Design-Spec `docs/superpowers/specs/` → `bauplan-archiv/` (byte-identisch); `docs/` weg.
  - Zwei Alt-Backup-Ordner `_wzs-*` (10 Dateien) entfernt; Wurzel-Allowlist als Test.
  - Kategorie `feature-manuals/` angelegt, erster Inhalt Jira-REST-Zugang (Zeiger, kein
    Secret — geprüft).
  - Ports (Opus): `standardprozesse/standardprozess-authoring.md`,
    `standardprozesse/wissens-router-bau.md`, `plugins/nc/referenz/wissens-router.md` — die
    Router aus Phase H hatten weder Bau-Norm noch gemeinsame Referenz.
  - `SECURITY.md` (Opus) aus `hooks.json` + Hook-Köpfen; Opt-out-Variablen per Codezeile belegt.
  - **Drift geheilt:** `AGENTS.md` Abschluss-Checkliste verlangte noch CHANGELOG-Eintrag +
    Bump vor jedem Push — Widerspruch zu §0 (Waypoint-Modell aus PR #25); Sync-Matrix und
    Pfad-Index-Klasse `changelog` ebenso.
- **Zurückgestellt auf Maintainer-Weisung:** Betriebshandbuch (Agent gestoppt, Entwurf nur im
  Scratchpad). **Abgelehnt (Overseer):** Sucheindex-Eintrag für die Referenz mit
  `basis: kern-plugin` — bräche die Regel „je lebender Quelle in Index-Teil 2".
- Sieben neue Registerzeilen (Betriebshandbuch, Router-Testlücken, Router-Doppelpflege,
  toter `reserve/*`-Zeiger, E5-Neubewertung, `firmenkernprozesse/`-Frage, LICENSE/CONTRIBUTING).
