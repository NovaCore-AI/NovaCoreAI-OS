# macOS
.DS_Store

# Project-scope dev CLAUDE.md (lokale Arbeitsregeln, nicht Teil des Plugins)
CLAUDE.md

# Nested git repositories (should be integrated as files or submodules)
*/.git

# Isolated agent/worktree workspaces
.worktrees/

# Sitzungswissen wohnt in diesem Repo in der Wissensbasis (knowledge-base/sitzungswissen/,
# Mapping D14 / Onsite §15.29 — SSOT-Residenzpflicht). Der frühere lokale Strom
# .nc/erinnerung/ ist mit Phase I aufgehoben; .nc/ bleibt hier ausschließlich als Schutz
# gegen alte Skill-Stände, die noch dorthin schreiben würden — NICHT als zugesagter
# Ablageort. Wer hier bewusst etwas tracken will (etwa ein paralleler Vorgang), nimmt es
# per `git add -f` auf und begründet das im PR.
.nc/
